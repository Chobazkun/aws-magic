{
  "name": "aws-magic",
  "version": "0.1.0",
  "description": "`aws-magic` is a CLI tool that leverages the OpenAI API to translate natural language descriptions into AWS CLI commands.",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Chobaz",
  "license": "ISC",
  "devDependencies": {
    "@types/inquirer": "^9.0.7",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "commander": "^11.1.0",
    "inquirer": "^8.2.6"
  },
  "bin": {
    "aws-magic": "./dist/aws-magic.js"
  }
}
